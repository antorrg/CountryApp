let token = ''
let userId = ''

export const setToken = (newToken) => {
  token = newToken
}

export const getToken = () => {
  return token
}

export const setId = (newid) => {
  userId = newid
}

export const getId = () => {
  return userId
}
